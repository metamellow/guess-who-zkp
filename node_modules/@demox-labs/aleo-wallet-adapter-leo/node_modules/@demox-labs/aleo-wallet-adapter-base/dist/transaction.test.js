import { Transaction, Transition } from './transaction';
import { assert } from 'chai';
describe('Transaction', () => {
    it('should create a transaction', () => {
        const address = 'address';
        const chainId = 'chainId';
        const program = 'program';
        const functionName = 'functionName';
        const inputs = ['inputs'];
        const provingKeyUrl = 'provingKeyUrl';
        const transition = new Transition(program, functionName, inputs);
        const transaction = new Transaction(address, chainId, [transition]);
        assert.equal(transaction, {
            address,
            chainId,
            transitions: [
                {
                    program,
                    functionName,
                    inputs,
                    provingKeyUrl,
                },
            ],
        });
    });
});
//# sourceMappingURL=transaction.test.js.map